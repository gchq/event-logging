/*
 * This settings file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 * In a single project build this file can be empty or even removed.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user guide at https://docs.gradle.org/3.5.1/userguide/multi_project_builds.html
 */

rootProject.name = 'event-logging'

include 'event-logging-api'
include 'event-logging-base'
include 'event-logging-generator'

//Use custom names for the build files
rootProject.children.each { project ->
    project.buildFileName = project.name + ".gradle"
}
